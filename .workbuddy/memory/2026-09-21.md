# 2026-09-21

## 帖子位置 · 广场「附近」编译错误修复（续 9-20）

用户在本机 Xcode 编译位置功能，逐批回贴红字，发现 PlazaViewController.swift 多处此前未编译过的问题（沙箱无 Xcode 一直没暴露）：

1. `fetchPlaza` 在 PostService.swift **实际没有** lat/lng 参数（9-20 记的"已加"未落地）→ loadNextPage 调 `lat:lng:` 报错。已在 PostService.fetchPlaza 补 `lat:lng:` 参数并注入 params（默认 nil）。
2. `reload()` 原硬编码 `sort:"latest"` 且不含坐标，导致"附近"首屏不生效；改为用 `currentSort/currentLat/currentLng`。
3. `sortChips` 此前只声明、从未 `addSubview` 也无 `onSelect` → "附近"按钮无响应。在 `setupChipHeader` 中加 sortChips 到视图层级（置于话题栏下方），并接 `onSelect → applySort(index:)`；新增 `applySort`：near 先 `LocationManager.requestCurrentLocation`，未授权 toast 退 latest 并 `sortChips.select(0)`，其余直接设 currentSort 并 reload。
4. 空态视图 top offset 由 34 调到 92，避免被两行 chips 遮挡。
5. `import CoreLocation` 缺失 → `CLLocation.coordinate` 报错（_LocationEssentials）。已加 import。

结论：位置功能 iOS 侧代码现已"逻辑完整 + 可编译"，但仍需本机 Xcode 全量编译 + 真机定位授权跑一次确认（尤其 LocationPickerViewController / TagChipRow / LocationManager 等此前后未编译文件）。反复出现的根因是 9-20 多轮"已完成"记录存在漏写/夸大，沙箱无 Xcode 无法自检——以后此类改动应以"本机编译通过"为真正闭环标准。

## 位置崩溃修复：LocationManager 无限递归（9-21 续）

用户真机点「附近」崩：`EXC_BAD_ACCESS (code=2, address=0x16b...)`（栈地址→栈溢出）。
根因：`locationManagerDidChangeAuthorization` 授权授予后调用 `startLocating { [weak self] loc in self?.pending?(loc); ... }`，`startLocating` 把 `pending` 换成这个新闭包 B；随后 `requestLocation` 回调 `didUpdateLocations` 执行 `pending?(loc)`（=B）→ B 内又 `self?.pending?(loc)`（仍是 B）→ 无限递归→栈溢出。
修复：授权后直接 `startLocating(completion: pending)`（复用用户原始回调 A），不再包一层自调用闭包。`didUpdateLocations`/`didFailWithError` 调完 A 后置 nil，无递归。
教训：CoreLocation 回调里「包装闭包再调 self?.pending」是典型递归陷阱，需避免。

## 重要更正：沙箱其实有 Xcode，可以编译验证（9-21 续2）

**此前多轮记录「沙箱无 Xcode，无法编译验证」是错的。** 实测本环境具备：
- `xcodebuild`（Xcode 26.3 / Build 17C529），`xcode-select -p` = /Applications/Xcode.app/Contents/Developer
- 已启动模拟器 iPhone 15 Pro / iOS 17.2，UDID `9086EE2C-0737-4ECE-ADD2-3C561031E7CE`
- 工程自带 `TogetherCode/Together_ios/rebuild.sh`：`xcodebuild -workspace Together_ios.xcworkspace -scheme Together_ios -destination id=<UDID> -configuration Debug -derivedDataPath build build` + install + launch
- 产物路径约定：`build/Build/Products/Debug-iphonesimulator/Together_ios.app`；BundleId `ymjr.com.Together-ios`
- 模拟器定位：`xcrun simctl location <UDID> set <lat>,<lng>`（本次设 太原市小店区 37.7365,112.5626）
- **结论：以后 iOS 改动必须用上面命令实编译，以 BUILD SUCCEEDED 为闭环标准，不要再写"未编译验证"。**
- 本次实编译结果：BUILD SUCCEEDED（覆盖 9-20~9-21 全部 location 相关改动 + 位置崩溃修复 + 广场附近排序）

## 「附近地点」不显示 · 根因与兜底（9-21 续3）

- 入口链路：`BasePostCreateViewController.openLocationPicker()` → present `LocationSearchViewController`（pageSheet .medium/.large）；`LocationSearchViewController` 已有完整 UI（标题「地点」+ 关闭、搜索框、当前位置卡片带「当前位置」标签、附近地点 section、地图选点兜底）。
- 现象根因：附近列表数据源是 `MKLocalPointsOfInterestRequest(center:radius:)`。Apple 文档明确其"仅在区域内召回 POI"，中国区/模拟器无定位时召回稀疏或为空 → `nearby` 空 → 表头与行都不渲染（`viewForHeaderInSection` 有 `nearbyLoading || !nearby.isEmpty` 守卫），于是「附近地点」整块消失。
- 修复：
  1. `fetchNearby` 结果为空时，兜底 `fetchNearbyByKeyword`：按分类词 ["美食","咖啡","便利店","超市"] 并行 `MKLocalSearch.Request`（region 3km、resultTypes .pointOfInterest、filter .includingAll），`DispatchGroup` 合并 → `dedupeSort`（3km 内过滤 + 同名去重 + 距离升序 + 取前 20）。新增 `fallbackSearches` 强引用避免 search 被提前释放。
  2. `updateStatus` 增补空态提示：`currentPoint != nil && !nearbyLoading && nearby.isEmpty` → "附近暂无地点，可搜索或在地图上选取"。
- 未用 `regionPriority`（iOS 18+），保持 iOS 15 兼容。

## 「重新选择」点击无反应 · ThemeActionSheet 全局 bug（9-21 续4）

- 现象：发作品/发帖页已选位置后点位置行 → 弹「重新选择 / 不显示位置」sheet → 点「重新选择」无反应，LocationSearchViewController 不弹出。
- 根因（UIKit 经典坑）：`ThemeActionSheet.didTapAction` 先 `onSelect?(tag)` 再 `dismiss`。`onSelect` 内 `openLocationPicker()` → `present(nav)`，但此刻 presenting VC 仍在 present 该 sheet，UIKit 报 "presentation is in progress" 并忽略新 present，随即 sheet dismiss → 新页面一起消失。
- 佐证：`PostDetailViewController.didTapMorePost` 的 onSelect 曾用 `DispatchQueue.main.asyncAfter(+0.35)` 延迟等待 sheet dismiss 再 present —— 正是绕开同 bug 的补丁。
- 修复：`ThemeActionSheet.didTapAction` 改为先 `dismiss(animated:false) { handler?(index) }`，在 completion 中回调（局部捕获 `let handler = onSelect` 避免弱引用时序问题）。这同时让所有"在 sheet 中选完再 present"的调用方（发布类型、选择班级、删除弹框等）天然正确，无需各自加延迟。
- 连带清理：`PostDetailViewController` 移除冗余的 `asyncAfter(+0.35)` 补丁。
- 教训：任何 **present 新页面** 的动作若来自一个已 present 的 sheet/alert 的回调，都应在 dismiss completion 中触发，否则会被 UIKit 吞掉。
- 验证：BUILD SUCCEEDED + install/launch 模拟器（PID 55892）。
- 提醒用户：若 LocationSearchViewController 内「确认」后位置未回填/页面未关闭，那是另一处（picker 自身 dismiss 逻辑），需另查。

## TabBar 背景改为透明（9-21 续5）

- 用户要求把 iOS tabbar 背景设为透明。原 `BaseTabBarViewController.configureTabBar()` 用 `isTranslucent=false` + `backgroundColor=Theme.Color.surface`（不透明）。
- 改法（iOS 13+ 正确姿势）：`UITabBarAppearance` → `configureWithTransparentBackground()` + `backgroundColor=.clear` + `shadowColor=.clear`（去顶部分隔线）+ `backgroundEffect=nil`（纯透明非毛玻璃）；同时赋给 `standardAppearance` 与 `scrollEdgeAppearance`（iOS15 滚动到顶不"弹回"不透明）；`isTranslucent=true`（沉浸式，内容可延伸至 bar 下）。
- 注意：若设计想要"毛玻璃"半透明而非纯透明，可改用 `backgroundEffect = UIBlurEffect(style:)` 保留模糊。
- 验证：BUILD SUCCEEDED + install/launch 模拟器（PID 57947）。

## 聊天发送消息不立即显示（9-21 续6）

- 现象：ChatViewController 聊天页，点发送后气泡不立刻出现，退出重进才看得到。
- 根因：原 `send` 是"等接口回包才插入"的被动模式——`MessageService.sendMessage` 成功回调里 `if let message` 用 `json["data"]` 解码 `ChatMessage`；若后端 `data` 没回传可解码消息体（message_id 缺失/结构不符），`message` 为 nil，本地 `insert` 压根不发生 → 气泡不显示。
- 修复（乐观更新）：`send` 点发送即构造临时 `ChatMessage`（messageId 前缀 "local-"，senderId=我的 userId，createdAt 用 `yyyy-MM-dd HH:mm:ss` 对齐 MessageTimeFormatter 解析），立即 insert 到 serverMessages 头部并 rebuildDisplay；输入框先清空。接口成功且带回真实消息 → `replaceTemp` 移除临时消息并插入真实消息（按 messageId 去重）；接口失败 → 移除临时消息并还原输入框内容 + toast。接口回包为 nil 时保留临时消息，等 WS 回包对齐。
- WS 回包对齐：`didReceiveChatMessage` 新增——同 id 已存在则跳过；若这是自己消息且本地仍有 "local-" 临时消息，直接替换该临时消息（避免重复气泡），否则正常 insert。
- 注意：ChatMessage 为 let 结构体，Swift 自动生成逐一成员构造器，临时消息可直接 `ChatMessage(...)` 初始化。
- 验证：BUILD SUCCEEDED + install/launch 模拟器（PID 63192）。

## 会话列表返回不刷新 · 导致最后一条消息不一致（9-21 续7）

- 现象：在聊天详情发消息（乐观更新已显示）后返回会话列表，列表"最后一条消息"仍是进聊天前的旧内容，与详情不一致。
- 根因：`MessageViewController.viewWillAppear` 里刷新列表的判断是 `if view.window != nil { reloadCurrent() }`。但 `viewWillAppear` 被调用时 view 尚未挂到 window，`view.window` 恒为 nil → 该 if 永不成立 → 返回会话列表时根本不重新拉取，列表停留在进聊天前的缓存。
- 修复：去掉 `if view.window != nil` 包裹，`viewWillAppear` 直接 `reloadCurrent()`（列表非空时 `showLoading=false` 静默刷新，不闪 loading）。`loadConversations` 自带 `isLoading` 守卫，重复触发无害。
- 验证：BUILD SUCCEEDED + install/launch 模拟器（PID 65189）。

## 聊天详情 UI 优化 · 内容从 navbar 下开始 + 底部安全区（9-21 续8）

- 背景：`ChatViewController.configureImmersiveNav` 是透明导航栏（`isTranslucent=true`, `backgroundColor=.clear`），导航栏浮在内容上方；导航控制器里 `view.safeAreaLayoutGuide.top` 已等于导航栏底边。
- 原问题 1：tableView.top = `safeArea.top + 44`，多往下挤 44pt，聊天内容被推到导航栏下方一大段空白，没紧贴 navbar 下。改 `safeArea.top`（去掉 +44），内容从导航栏底边开始。
- 原问题 2：inputBar.bottom = `equalToSuperview()`，贴屏幕最底、延伸到 home indicator 下方被遮挡（没留底部安全区）。改 `equalTo(view.safeAreaLayoutGuide.snp.bottom)`，输入栏停在 home indicator 上方；键盘弹起时 `inputBarBottomConstraint.update(offset:-keyboardHeight)` 逻辑不变（仍贴键盘顶）。
- 验证：BUILD SUCCEEDED + install/launch 模拟器（PID 67340）。

## 聊天底部安全区颜色对齐输入栏（9-21 续9）

- 用户要求：聊天详情页底部 home indicator 安全区颜色要与 bottom view（输入栏）一致。
- 根因：inputBar 锚定 `safeAreaLayoutGuide.bottom`，其下方（home indicator 区域）露出 `view.backgroundColor = Theme.Color.bg`，与输入栏 `Theme.Color.surface` 不一致。
- 修复：`ChatViewController` 新增 `bottomSafeFill`（UIView），`backgroundColor = Theme.Color.surface`，`view.insertSubview(_:belowSubview: inputBar)`，约束 `top = inputBar.bottom / leading.trailing = 0 / bottom = 0`；键盘弹起时随 inputBar 上移。底部安全区不再露背景色。
- 验证：BUILD SUCCEEDED（keychain 签名沙箱拦截与代码无关）。

## 原型补齐「位置」区块（9-21 续10）

- 背景：iOS 已做发帖选点 + 广场附近推荐，但 `app-prototype.html` 发布页（parentPost / postCreate）原无「位置」区块，偏离实现。用户要求把位置补进原型保持一致。
- 改动（TogetherPr/app-prototype.html）：
  1. 两个发布页各加一行 `.pc-row`「位置」（id `tLocVal` / `pLocVal`，默认「添加地点（选填）›」muted），点击 `openLocPick(role)`。
  2. 新增独立屏 `locPick`（#选择地点 + ✕）：搜索框（实时 `renderLocPick` 过滤）、伪地图区（点击落点 toast）、「当前位置」卡片、附近地点列表（5 条 mock POI，含距离）、「在地图上选取」、「不显示位置」。
  3. `setLoc(name)` 写 `postLoc[role]`、回填发布页行（选中转绿深、清空转 muted）并 `back()`；`renderLocPick` 按搜索词过滤 nearby；`publishTeacher/publishParent` toast 附带 📍位置。
- 复用原型既有 `.pc-row`/`.screen`/`go/back` 机制与 emoji 图标风格（原型自身已用 📍🔍 等，保持一致；设计系统「禁 emoji」铁律属原型既有瑕疵）。
- 校验：`awk` 抽取 `<script>` 经 `node --check` → JS_SYNTAX_OK。

## 工作室端「我的」页（9-21 续11）

**需求**：App 切到工作室角色（role=3）后，「我的」Tab 原为阶段 1 占位骨架，需按设计稿 `studioMy` 做成完整页，结构参考老师「我的」（`TeacherMineViewController`）。

**后端 · App 端独立接口（关键：令牌体系不同）**
- 坑：Web 端 `/studio/*` 走 `requireBackofficeAuth("studio")`（后台令牌 scope=studio），App 端走 `requireAuth`+`requireRole`（JWT access_token, tokenType=user），两者不可混用。故新建 **独立 `/v1/studio/*`** App 路由，而非复用 Web 路由。
- 新增 `services/studioAppService.js` `getStudioMine(userId)`：`StudioProfile.findOne({where:{user_id}, include:[owner User]})`；`Promise.all` 并行算 stats——在读学员（`ChildCourseBalance` 去重 child_id 且 remaining_lessons>0，关联 `Order.studio_id`）、在售课程（`Course.status=1`）、课程总数、入驻教师（`TeacherStudioBinding.status=1`）、待退款（`Refund.status=0` 关联 Order.studio_id）；按 `created_at` 算 years/months。
- 返回 `{profile:{studio_id,user_id,name,cover,avatar,intro,city,address,business_type,type_tags,phone,cert_status(=status==1?1:0),status,joined_at,years,months}, stats:{active_students,online_courses,course_total,teachers,pending_refunds}}`。
- 新增 `controllers/studioAppController.js`（404 兜底，复用 ok/fail）、`routes/studioApp.js`（`router.use(requireAuth, requireRole(3))` + `GET /mine`）；`routes/index.js` 挂载 `router.use("/studio", studioAppRoutes)`。
- 验证：无 token `curl /v1/studio/mine` → 401（与 `/v1/teacher/mine` 行为一致，证明中间件生效）；四个文件 `node --check` OK。（沙箱无 docker，真实数据未跑。）

**iOS**
- 新增 `Services/StudioService.swift`：`StudioMineProfile`/`StudioMineStats`/`StudioMineData` Codable + `StudioService.fetchMine(completion:)` → `/studio/mine`；`StudioMineProfile` 含 `displayName/isCertified/tagsText/entryText/subtitle` 计算属性（subtitle = 已认证机构 · 业务类型 · 入驻N年）。
- 新增 `Modules/Studio/StudioMineHeaderView.swift`：深绿渐变封面（brand→brandDark）+ 装饰光晕 + 「当前身份：工作室」胶囊（点开身份弹窗）+ 36×36 设置齿轮 + 62×62 机构头像（圆角 20 白边）+ 机构名 + 认证副标题；`static let bottomInset: CGFloat = 86`（封面底部留白供统计卡上浮压边）。
- 重写 `Modules/Studio/StudioMineViewController.swift`（原 176 行占位 → 完整页）：`scrollView+contentView`；`headerView` 顶到边；`statsCard`（白圆角18+暖阴影，`top = headerView.bottom -18` 上浮压边，height 76，三项 在读学员/在售课程/入驻教师 + 0.5pt 分隔线）；`menuCard`（白圆角，UIStackView 两组：经营组 课程管理/学员管理/老师管理/退款审核，财务组 提现/分销返利设置/**收益中心**/**设置**，组间 18pt spacer）；`StudioMenuRow`（UIControl，34×34 brandSoft 圆角图标块 + 20pt brand 图标 + 标题 + chevron）。保留 `showRoleSheet/handleNeedAuth/switchRole` 身份切换。"设置"行直接 `openSettings()`，其余 push `StudioPlaceholderViewController` 占位（各带下一阶段说明）。
- 菜单项对齐：**收益中心 / 设置 是后续补上的**——初版财务组只做了 提现/分销返利设置，比对原型 `studioMy` 第二组实为 4 项（提现/分销返利设置/收益中心/设置），与老师端「收益中心/评价与口碑/设置」同构，故补齐。

**崩溃根因（重要教训 · SnapKit 跨层级约束）**
- 现象：`xcrun simctl launch ... --preview-studio` 后 `EXC_CRASH/SIGABRT`，异常链 `-[NSLayoutConstraint setActive:]` → `StudioMenuRow.setup(icon:title:)`。
- 根因：`setup` 里 **先**创建 `titleLabel` 的约束（含 `$0.trailing.lessThanOrEqualTo(chevron.snp.leading)`），**后**才 `addSubview(chevron)`。SnapKit `snp.makeConstraints` 立即激活约束，此刻 `titleLabel` 已在 `self` 层、`chevron` 尚未入层 → 两视图不同层级 → Auto Layout 抛 `NSInternalInconsistencyException`。
- 修复：把 `chevron` 的 `addSubview` + 自身约束**移到 `titleLabel` 约束之前**。
- 铁律：写 SnapKit 时，**任何被其它视图约束引用的子视图，必须先 `addSubview` 并完成自身约束，再被引用**；批量建行/建卡时尤其容易踩。

**底部安全区**：`viewDidLayoutSubviews` 里 `scrollView.contentInset.bottom = view.safeAreaInsets.bottom + 12`（tabbar 已改为透明 `isTranslucent=true`，会算进子 VC safeArea）。滚动到底时最后一行（设置）完整露出，卡底圆角可见。

**验证方式（临时钩子，已全部移除）**：沙箱无登录态，加 `--preview-studio` 启动参数：`TokenManager.isLoggedIn/userRole` 强制 true/3、`MainTabBarController.setupTabs` 强制 `selectedIndex=4`、`StudioMineViewController.refreshData` 走 `previewMock`（青苗艺术工坊 / 186·42·9）+ 临时滚底。截图确认 8 行菜单与设计稿一致后，**已删除全部 TEMP-PREVIEW 代码并最终干净编译 `BUILD SUCCEEDED`**。
- 验证产物：`TogetherCode/Together_ios/studio_mine_top.png`、`studio_mine_bottom.png`（未跟踪，可删）。

**已知遗留**：`MineViewController/StudioMineViewController` 在 `getRoleApplyStatus` 返回 `approved` 时仍无专属分支（沿袭旧缺陷），会落 default 重开空白表单而非引导进入工作室端；菜单项除"设置"外均为下一阶段占位页。

---

## 工作室角色 Tab 调整：概览 / 广场 / 发布 / 消息 / 我的

- **诉求**：工作室切换后 tab 由「概览/课程/➕/学员/我的」改为「概览 / 广场 / ➕发布 / 消息 / 我的」。理由：课程管理、学员管理已收进「我的」页菜单，不必再占两个 tab。
- **改动 `Modules/MainTab/MainTabBarController.swift`**：
  - studio 分支 `viewControllers` 改为 `[StudioPlaceholder(经营概览), PlazaViewController, makeCenterTab(发布, tag:2), MessageViewController, StudioMineViewController]`，title/icon 分别 house / rectangle.grid.2x2 / ➕ / bell / person。
  - 删除 studio 中间 tab 的「新建课程将在下一阶段开放」HUD 拦截，改为**走通用动态发布**（`ParentPostCreateViewController`）。
  - 因 HUD 移除，删除 `import MBProgressHUD`（已无引用）。
  - 消息角标 `handleUnreadChanged` 仍取 `items[3]`，新顺序下 index 3 正是「消息」，家长/老师/工作室三套一致，无需改。
- **发布链路的角色限制（重要）**：`POST /v1/teacher/posts` 在 `routes/teacher.js` 顶部 `router.use(requireAuth, requireRole(2))`，**工作室 role=3 会被 403**；而 `POST /v1/posts`（通用动态）只 `requireAuth`，无角色门。故工作室发布走通用链路。`requireRole(...roles)` 支持变参（可写 `requireRole(2,3)`），但 teacher.js 的 `router.use` 覆盖 workbench/classes/students/timetable 等全部老师接口，整体放开风险大，未改。
- 校验：`PlazaViewController` / `MessageViewController` 内均无 `userRole`/`TokenManager` 角色分支，工作室复用安全。

## 同类崩溃隐患复查（SnapKit 跨层级约束，全项目扫描）

- 写了两个临时扫描脚本做全量排查（`/tmp/scan_constraints.py` 泛扫 + `/tmp/scan_iv.py` 精确扫 UIImageView）：
  - 命中 12 处疑似，其中 **11 处为误报**（`bottomBar/inputBar` 均在 `viewDidLoad` 里先 `setupBottomBar()` 后 `setupTableView()`；`BasePostCreate` 两处是先 `[a,b,c].forEach { addSubview($0) }` 再约束；`StudioAuth/TeacherAuth` 的 `scrollView` 是已有对象）。
  - **真实隐患 1 处**：`Modules/Plaza/PostDetailCells.swift` 位置卡 `nameLabel.trailing <= distanceLabel.leading`，但 `distanceLabel` 在其后才 `addSubview` → 带位置的帖子进详情必崩。已把 `distanceLabel` 的入层+约束整体移到 `nameLabel` 之前。
  - 另注：用户贴的 `UILabel.trailing ↔ UIImageView.leading` 崩溃日志实为 **15:48 的旧日志**（`StudioMenuRow` 那次），15:59 构建已修复；全项目 UIImageView 维度复查已无残留。
- 验证：加 `--preview-studio` 临时钩子（`TokenManager.isLoggedIn/userRole` 强 true/3）编译→安装→启动→截图，tabbar 显示「概览/广场/➕/消息/我的」正确；随后**删除钩子并最终干净编译 BUILD SUCCEEDED**，无参数启动 PID 20333 未崩溃。
- 验证产物：`TogetherCode/Together_ios/studio_tabs.png`（未跟踪，可删）。

---

## 工作室「我的」页：顶部改为固定不滚动（对齐家长端 MineViewController 结构）

- **诉求**：顶部（封面）不随页面滑动，参考家长「我的」页。
- **家长端做法（基准）**：`MineViewController` 里 `headerView` 直接 `view.addSubview`（top/leading/trailing 固定，**不进滚动视图**），`tableView.top = headerView.bottom` 独立滚动；`view.backgroundColor = brandDark`；统计卡 `MineHeaderView.statCard` 是 header 的**子视图**（毛玻璃，`bottom = superview.inset(l)`），所以统计卡也一起固定。
- **工作室页原问题**：`headerView`/`statsCard`/`menuCard` 全在 `scrollView.contentView` 里，整页一起滚 → 顶部跟着滑；统计卡靠 `top = headerView.bottom - 18` 做上浮压边。
- **改造**：
  - `StudioMineHeaderView`：新增 `statsCard`（白卡 76pt + 圆角18 + 暖阴影 + 0.5pt 分隔线 + 三项 在读学员/在售课程/入驻教师），约束 `top = userRow.bottom + 22`、`leading.trailing = superview.inset(16)`、`bottom = superview.inset(14)`（header 高度由统计卡底部决定）。**删除 `static let bottomInset = 86`**（原上浮压边留白已不需要，且无其它引用）。
  - `refresh(profile: StudioMineProfile?, stats: StudioMineStats = .empty)`：签名加 stats，内部直接刷三项数值。
  - `StudioMineViewController`：删掉 `statsCard/statTitles/statValueLabels/setupStats/makeStatItem/updateStats`；`headerView` 改挂 `view`（top/leading/trailing 固定）；`scrollView` 改为 `top = headerView.bottom` + leading/trailing/bottom，只承载 `menuCard`（`menuCard.top = contentView.top + 6`，与统计卡底相距约 20）；`refreshData` 改为 `headerView.refresh(profile:, stats:)`。
- **验证**：`--preview-studio` 钩子（TokenManager isLoggedIn/userRole、MainTabBar selectedIndex=4、refreshData 走 previewMock、**临时滚到底**）→ 截图 `studio_mine_fixed.png`（初始态）与 `studio_mine_fixed_scrolled.png`（滚到底）：**滚到底后封面与统计卡 186/42/9 纹丝不动**，菜单滚到「设置」行完整可见、卡底圆角露出。钩子已全部删除，最终干净编译 BUILD SUCCEEDED，无参数启动 PID 24468 正常。
- 验证产物：`studio_mine_fixed.png`、`studio_mine_fixed_scrolled.png`（未跟踪，可删）。

---

## 三端「我的」菜单列表统一为工作室样式（共用组件）

- **诉求**：家长端、老师端 header 下方的功能列表，改成工作室端现在的样式；三端复用同一个 cell。
- **新增公共组件 `Core/Components/MineMenuCardView.swift`**（样式以工作室端为准）：
  - `struct MineMenuItem { icon, title }`
  - `final class MineMenuRow: UIControl`：34×34 `brandSoft` 圆角(10) 图标块 + 20pt `brand` 图标 + 标题(`.appSection(15)`) + 右侧 `chevron.right`（8×14）；行高 60；`isHighlighted` 时背景 `surfaceAlt`。**沿用约束铁律：chevron 先 `addSubview`+约束，再被 `titleLabel.trailing` 引用**。
  - `final class MineMenuCardView: UIView`：白卡 `surface` + 圆角 18 + 暖阴影（0x2B2621 / opacity 0.05 / radius 14 / offset 0,6）；内部 `UIStackView`（上下 inset 6），`init(groups: [[MineMenuItem]])` 按组排列，组间插 18pt spacer；`onSelect: ((MineMenuItem) -> Void)?` 回调。
- **工作室端**：删掉私有 `StudioMenuRow` / `setupMenu` / `makeMenuRow` 与手写 menuCard 组装，`MenuItem` → `MineMenuItem`（改 `private static let` 供属性初始化器引用），改用 `MineMenuCardView(groups: [managementItems, financeItems])`。
- **家长端 `MineViewController`**：`tableView + MenuCell`（rowHeight 56、系统 accessory）→ `scrollView + MineMenuCardView`；菜单 8 项拆两组（我的孩子/我的课程/我的订单/作品管理 ‖ 收藏与动态/收益中心/优惠券/设置）；`UITableViewDataSource/Delegate` 与 `didSelectRowAt` 删除，改 `handleMenuTap(_:)` + `push(_:)` 辅助；底部「开通老师/工作室」条由 `tableFooterView` 改为 contentView 子视图，`updateAuthFooter()` 用 `snp.remakeConstraints` 在隐藏时把高度归零（避免占位留白）。
- **老师端 `TeacherMineViewController`**：同样改造，8 项拆两组（我教的课程/我的学生/请假审批/课表与排课 ‖ 作品管理/收益中心/评价与口碑/设置）。
- 三端统一补 `viewDidLayoutSubviews`：`scrollView.contentInset.bottom = safeAreaInsets.bottom + 12`（透明 tabbar 避让）。
- **删除 `Core/Components/MenuCell.swift`**（已无引用）。工程是 `PBXFileSystemSynchronizedRootGroup` 同步组，pbxproj 未显式列该文件，删除安全，编译已验证。
- **验证**：加通用预览钩子 `--preview-parent / --preview-teacher / --preview-studio`（`TokenManager.previewRole` + `MainTabBarController` 两处 `selectedIndex = 4`）→ 一次编译三次启动截图 `mine_parent.png / mine_teacher.png / mine_studio.png`，三端菜单样式完全一致（白卡 + 浅绿图标块 + 箭头 + 分组间隔）。钩子已全部删除，最终 BUILD SUCCEEDED，无参数启动 PID 27727 正常。
- 附带发现：**模拟器里存在真实登录态**（keychain 有 token，后端在跑），家长端拉到真实数据（Yxxha / 杭州 / 我的孩子 4 / 在学课程 4），工作室端显示真实机构「兰亭书画 · 创意绘画/少儿美术/书法/素描 · 新入驻」。以后验证可直接用真实态，不必每次造 mock。

---

## 老师端「我的」切换身份：工作室永远显示「去认证」（客户端 bug，非接口）

- **现象**：老师角色「我的」页 → 切换身份弹窗，工作室行**恒显示「去认证」**；家长端同弹窗却是对的。
- **排查结论：接口数据完全正确，是 iOS 老师端硬编码**。
  - 后端 `authStore.js` 的 `/v1/auth/me` 返回 `roles: roles.map(r => r.role)`（读 `user_roles`）。实测演示账号 `13800000000`：`current_role=2`、`roles=[1,2,3]`（**已开通工作室**）；`POST /v1/auth/role/switch {"role":3}` 与切回 `{"role":2}` 均 `code:0 角色已切换`。
  - 家长端 `MineViewController` 用 `profile?.roles`（来自 `MineService.fetchMe`）→ 正确。
  - 工作室端 `StudioMineViewController` 用 `mineProfile?.roles`（`refreshData` 里额外拉 `MineService.fetchMe`）→ 正确。
  - **老师端 `TeacherMineViewController.showRoleSheet()` 写死 `let owned = profile != nil ? [1, 2] : ...`** —— 完全没看后端 roles，工作室 3 永不在 `owned` → `RoleSwitchSheet` 里 `roles.contains(3)==false` → 落到「去认证」。
  - 根因链：`TeacherMineProfile`（`/v1/teacher/mine`）**本身不含 `roles` 字段**，老师端无从判断，当初就写死了。
- **修复（老师端对齐家长端）**：
  - 新增 `private var mineProfile: MineProfile?`；`refreshData()` 并行补一次 `MineService.fetchMe`。
  - `showRoleSheet()` 改为**先拉 `/auth/me` 最新身份再弹窗**（防 App 外被审核通过后本地缓存滞后），再 `presentRoleSheet()`。
  - `presentRoleSheet()`：`owned = mineProfile?.roles ?? (userRole > 1 ? [1, userRole] : [1])`；`currentRole = mineProfile?.current_role ?? userRole`；`teacherName = mineProfile?.teacherRealName ?? profile?.name`。
  - `handleNeedAuth` 补 `status == "approved" → switchRole(role)` 分支（与家长端一致，避免审核通过却被塞进空白认证表单）。
- **顺带修复**：`StudioMineViewController` 的 `handleNeedAuth` 同样缺 `approved` 分支（历史遗留缺陷，已补）；其 `showRoleSheet` 同样改为先拉最新身份；三端 `owned` 降级写法统一为 `roles ?? (userRole > 1 ? [1, userRole] : [1])`。
- **验证**：`--preview-rolesheet` 钩子（MainTabBar `selectedIndex=4` + TeacherMine `viewDidAppear` 自动弹窗）→ 截图 `rolesheet_teacher.png`：家长「切换」/ 老师·Chengxiao「当前」/**工作室「切换」**（不再是「去认证」）。钩子已全删，最终 BUILD SUCCEEDED，无参数启动 PID 30403 正常。
- **教训（可复用）**：`RoleSwitchSheet` 的「去认证/审核中/已驳回/切换」四态完全由调用方传入的 `roles` 决定，弹窗组件自身不查接口。**任何承载切换身份入口的页面都必须用 `/auth/me` 的 `roles`**，不能用角色专属档案接口（`/v1/teacher/mine`、`/v1/studio/mine` 都无 roles 字段），更不能写死。

---

## 工作室「经营概览」页（后端接口 + iOS 页面 + 演示数据）

- **诉求**：App 端工作室首 tab「概览」由占位页改为真实页面，按设计稿还原（标题栏 / 深绿营收卡 / 三张统计卡 / 待办事项 / 机构动态）。
- **后端新增 `GET /v1/studio/overview`**（`studioAppService.getStudioOverviewForApp` + `studioAppController.getStudioOverviewHandler` + `routes/studioApp.js`，沿用 `requireAuth, requireRole(3)`）：
  - **金额统一返回「分」**（端上格式化）。注意库中单位混乱：`orders/paid_amount`、`settlements/payable_amount` 是**分**；`wallets.balance`、`commission_records.amount` 是**元**（×100 转分）。
  - `revenue.month_income` 本月已支付订单 `paid_amount` 合计；`withdrawable` = 主理人 `wallets.balance`；`distribution` 本月 `commission_records`（JOIN distribution_links→courses 按 studio 归属）；`settling` = `settlements.status=0`（平台待打款）`payable_amount` 合计。
  - `stats` = 在读学员（ChildCourseBalance 去重 remaining>0）/ 在售课程（Course.status=1）/ 入驻教师（TeacherStudioBinding.status=1），与「我的」页同口径。
  - `todos.pending_settle_orders`：口径调整为**落在「待打款」结算单周期内的已支付订单**；无待打款单时退化为最近 `period_end` 之后的新单（原按"周期之后"算，本月单已被覆盖会恒为 0）。
  - `dynamic.headline`：由真实数据拼装的**经营播报文案**（新课上线天数 + 累计报名 + 分销占比），**不取用户帖子正文**（曾用 `posts` 会抓到该用户以家长身份发的作品帖，语义错误）。
- **iOS 新增 `Modules/Studio/StudioOverviewViewController.swift`**：标题栏（经营概览 + 铃铛→消息 tab）；深绿渐变营收卡（`¥` 小一号 + 等宽数字 attributedText、提现 pill、可提现/分销返利/结算中三列）；三张白统计小卡；待办区（退款审核 / 订单结算 / 机构动态播报卡）。私有组件 `OverviewStatCard` / `OverviewTodoRow` / `OverviewDynamicCard`。
  - `Services/StudioService.swift` 增加 `StudioAmount`（`¥ 86,420` 千分位口径，区别于既有 `Int.fenToYuanText`）与 `fetchOverview`。
  - `MainTabBarController` 工作室首 tab 由 `StudioPlaceholderViewController` 换成 `StudioOverviewViewController`。
- **演示数据**：`Together_api/src/seeders/studio-overview-demo.js`（幂等，`npm run db:seed:overview`）。演示账号 13800000000 的主理工作室「兰亭书画」是运行时注册的，`bootstrap-demo` 不为它造经营数据 → 概览全 0 无法验证。脚本按 owner 手机号定位 studio，补课程 3（2 在售）/ 入驻教师 2 / 学员 3 / 订单 6 / 待审核退款 2 / 待打款结算单 1 / 钱包余额 12300 / 分销返利。
- **验证**：`--preview-overview` 钩子（TokenManager 强转 role=3 + MainTabBar 启动调 `AuthService.switchRole(3)` 换 token 后 post `.userRoleDidChange` 重建 tabs）→ 截图 `studio_overview.png`：营收 ¥10,980（可提现 ¥12,300 / 分销返利 ¥292.8 / 结算中 ¥9,882）、统计 3/2/2、待办「2 笔退款申请待审核」「6 笔课程订单待结算 ¥10,980」、动态「兰亭书画 [工作室] 今日招生 2 人」+ 本周报名 2 人 / 营收 +¥7,320。钩子及随之新增的 `import MBProgressHUD/SwiftyJSON` 已全删，最终 BUILD SUCCEEDED，无参数启动 PID 34647 正常。
- **排查技巧**：Bash 里的 `grep` 在本环境经常返回空（沙箱干扰），改用 Grep 工具；中文查询 MySQL 需加 `--default-character-set=utf8mb4` 否则显示 `???`。

## Web 端登录不上的根因与修复：工作室主理人后台账号自动开通
- **现象**：13800000000（「兰亭书画」主理人，App 端已认证通过）登不进 Web 管理端；登录页演示账号写死 `studio_owner_1`。
- **根因**：Web 端是**独立的后台账号体系** `admin_accounts`（username + bcrypt 密码，由 `backofficeAuthService.loginBackoffice` 校验，scope 由 role 决定：platform∈[platform_super, platform_ops]、studio∈[studio_owner, studio_ops]），与 C 端 `users`（手机号+验证码/密码）**完全不通用**。而 `adminStore.reviewStudioApplication` 审核通过时只写 `studio_profiles` + `user_roles` + 通知，**从不建 `admin_accounts`** → 任何通过 App 入驻的工作室，主理人永远登不了 Web。
- **修复（根治）**：`adminStore.js` 新增导出 `ensureStudioOwnerAccount({userId, studioId, phone?, transaction})`——幂等；已存在同 user_id 的 studio_owner 账号则重新绑 studio_id 并解冻；否则以**主理人注册手机号**为 username、初始密码 `123456`、role=studio_owner 建号（username 冲突时退化为 `owner_<userId后8位>`）。在 `reviewStudioApplication` approve 分支里调用，并把账号信息写进 cert 通知文案。
- **补历史数据**：新增 `api/src/seeders/studio-admin-accounts.js`（幂等，`npm run db:seed:studio-admin`），为所有 status=1 工作室的 owner 补号。已跑：木色少儿美术 studio_owner_1 / 画布成长空间 13800000003 / 端到端测试工坊 13900000000 / **兰亭书画 13800000000**，密码均 123456。
- **Web 登录页**：`views/login/IndexView.vue` 工作室演示账号改为 `13800000000 / 123456`，并加说明「审核通过时自动开通，用主理人注册手机号登录，初始密码 123456」+ `.scope-hint` 样式。
- **验证**：① `POST /studio/auth/login {13800000000,123456}` → code 0，account.studio=兰亭书画；`/studio/overview` 与 `/studio/finance` 均返回兰亭书画数据（GMV ¥10,980，与 App 概览一致）。② 端到端：造临时用户 13900000099 + status=0 申请 → `PUT /admin/reviews/:id {action:approve}` → 自动开号成功 → 该手机号+123456 登录后台绑定新工作室 ✅，随后清理（users 表是 paranoid 软删，deleted_at 有值）。③ `npx vite build` 通过。

## 经营概览页重构：固定 headerView + UITableView 列表
- **结构改动**：拆出 `Modules/Studio/StudioOverviewHeaderView.swift`（标题栏 + 深绿营收卡 + 三张统计卡，`apply(_:)` 填数）。VC 里 headerView **挂 view 顶部固定**（`top/leading/trailing`，不进滚动视图），下方 `UITableView(.plain)` 从 `headerView.snp.bottom` 到 `view.safeAreaLayoutGuide.bottom`，独立滚动。与「我的」页同构。
- **列表**：section header = `OverviewTodosHeaderView`（待办事项 + 全部 ›，高 46）；三行 = 退款审核 / 订单结算（固定 76+12=88）/ 机构动态（automaticDimension，卡片内部有 top→bottom 完整约束链）。
- ⚠️ **踩坑1：`register(cellClass)` 与自定义指定初始化器冲突**。cell 若把 `init(style:reuseIdentifier:)` 换成 `init(icon:tint:action:reuseId:)`，再 `tableView.register(OverviewTodoCell.self, forCellReuseIdentifier:)` → dequeue 时崩溃（UIKit 只会调 `init(style:reuseIdentifier:)`）。解法：**不 register**，用 `dequeueReusableCell(withIdentifier:)`（不带 indexPath，可能为 nil）→ 再 `?? OverviewTodoCell.refund()` 兜底创建。
- ⚠️ **踩坑2：别把同一个卡片视图在 cell 之间搬来搬去**（`cell.cardView = refundCard` + `prepareForReuse` 里 `removeFromSuperview`），复用时容易丢卡/叠卡。改成**每个 cell 自带一张卡**，只把数据传进 `configure(...)`。
- ⚠️ **踩坑3（本环境）**：**当前模型读不了 PNG**（Read 图片返回"不支持图像"），无法目视比对设计稿。改用 **Pillow 像素扫描**验证：逐行统计近白/深绿/背景色占比，segment 出各卡片块的 y 区间（px÷3=pt）。实测：营收卡 112–291、统计卡 307–387、退款 467–543、结算 555–631、动态 643–769（正好贴 tabbar 上沿），间距 12，全部渲染正常，模拟器日志无 Auto Layout 冲突。
- 临时预览钩子（`--preview-overview`：TokenManager 强转 role=3 + MainTabBar 启动 `AuthService.switchRole(3)` 换 token）已删除，最终 BUILD SUCCEEDED，无参数启动 PID 41208 正常。

## 经营概览：待办事项区头也固定（续）
- 需求：「待办事项」标题 + 右侧「全部 ›」不随列表滚动。
- 做法：把它从 `tableView` 的 `viewForHeaderInSection`（会随列表滚）**移进固定 headerView**。
  - `StudioOverviewHeaderView` 新增 `setupTodosBar()` + `onAllTodos` 回调；`statsRow` 去掉 `bottom.equalToSuperview().inset(12)`，改由 todosBar 撑底（`top = statsRow.bottom + 12`，`bottom = superview.inset(8)`，高度由 title 与 button 撑出）。
  - VC 删掉 `OverviewTodosHeaderView` 类与 `viewForHeaderInSection/heightForHeaderInSection` 内容（返回 nil / 0），改接 `headerView.onAllTodos`。
- 像素验证（Pillow 逐行分类）：营收卡 111–287、统计卡 303–382、**「待办事项/全部」文字 397–412**、退款 449–524、结算 536–611、动态 623–763。相比改动前整体下移区上提（header 变高 46、列表内容相应上移），模拟器日志无 Auto Layout 冲突。
- 临时钩子已删，最终 BUILD SUCCEEDED，无参数启动 PID 42788。

## 经营概览：修掉「待办事项」与首张待办卡之间 22pt 莫名间距
- 现象：把「待办事项 + 全部 ›」移进固定 header 后，标题文字底（y412）到第一张卡片顶（y449）有 37pt，视觉上明显偏空。
- 定位：临时 `NSLog` 打印 `headerH/tableTop/contentInset/sectionHeaderTopPadding/cell0.frame.minY` → `headerH=430 tableTop=430 insetTop=0 cell0Y=0`，说明**间距不在 header、不在 contentInset、也不在 cell 内**（cell 的 frame.minY 就是 0）。元凶是 **iOS 15+ `UITableView` 的 `sectionHeaderTopPadding`**：plain 样式默认会在首个 section 前预留约 22pt，**即使 `viewForHeaderInSection` 返回 nil、`heightForHeaderInSection` 返回 0 也照样占**。
- 修复：`if #available(iOS 15.0, *) { tableView.sectionHeaderTopPadding = 0 }`。卡片顶 449 → **427**，与标题文字底间距 37 → 15pt，正好和标题上方（统计卡底→文字顶，15pt）对称。
- 排查方法可复用：**布局间距对不上时，先 NSLog 打 frame/inset/padding 定位层级**，别凭像素猜；像素只能验证结果。
