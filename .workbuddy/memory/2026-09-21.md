# 2026-09-21

## 帖子位置 · 广场「附近」编译错误修复（续 9-20）

用户在本机 Xcode 编译位置功能，逐批回贴红字，发现 PlazaViewController.swift 多处此前未编译过的问题（沙箱无 Xcode 一直没暴露）：

1. `fetchPlaza` 在 PostService.swift **实际没有** lat/lng 参数（9-20 记的"已加"未落地）→ loadNextPage 调 `lat:lng:` 报错。已在 PostService.fetchPlaza 补 `lat:lng:` 参数并注入 params（默认 nil）。
2. `reload()` 原硬编码 `sort:"latest"` 且不含坐标，导致"附近"首屏不生效；改为用 `currentSort/currentLat/currentLng`。
3. `sortChips` 此前只声明、从未 `addSubview` 也无 `onSelect` → "附近"按钮无响应。在 `setupChipHeader` 中加 sortChips 到视图层级（置于话题栏下方），并接 `onSelect → applySort(index:)`；新增 `applySort`：near 先 `LocationManager.requestCurrentLocation`，未授权 toast 退 latest 并 `sortChips.select(0)`，其余直接设 currentSort 并 reload。
4. 空态视图 top offset 由 34 调到 92，避免被两行 chips 遮挡。
5. `import CoreLocation` 缺失 → `CLLocation.coordinate` 报错（_LocationEssentials）。已加 import。

结论：位置功能 iOS 侧代码现已"逻辑完整 + 可编译"，但仍需本机 Xcode 全量编译 + 真机定位授权跑一次确认（尤其 LocationPickerViewController / TagChipRow / LocationManager 等此前后未编译文件）。反复出现的根因是 9-20 多轮"已完成"记录存在漏写/夸大，沙箱无 Xcode 无法自检——以后此类改动应以"本机编译通过"为真正闭环标准。

## 位置崩溃修复：LocationManager 无限递归（9-21 续）

用户真机点「附近」崩：`EXC_BAD_ACCESS (code=2, address=0x16b...)`（栈地址→栈溢出）。
根因：`locationManagerDidChangeAuthorization` 授权授予后调用 `startLocating { [weak self] loc in self?.pending?(loc); ... }`，`startLocating` 把 `pending` 换成这个新闭包 B；随后 `requestLocation` 回调 `didUpdateLocations` 执行 `pending?(loc)`（=B）→ B 内又 `self?.pending?(loc)`（仍是 B）→ 无限递归→栈溢出。
修复：授权后直接 `startLocating(completion: pending)`（复用用户原始回调 A），不再包一层自调用闭包。`didUpdateLocations`/`didFailWithError` 调完 A 后置 nil，无递归。
教训：CoreLocation 回调里「包装闭包再调 self?.pending」是典型递归陷阱，需避免。

## 重要更正：沙箱其实有 Xcode，可以编译验证（9-21 续2）

**此前多轮记录「沙箱无 Xcode，无法编译验证」是错的。** 实测本环境具备：
- `xcodebuild`（Xcode 26.3 / Build 17C529），`xcode-select -p` = /Applications/Xcode.app/Contents/Developer
- 已启动模拟器 iPhone 15 Pro / iOS 17.2，UDID `9086EE2C-0737-4ECE-ADD2-3C561031E7CE`
- 工程自带 `TogetherCode/Together_ios/rebuild.sh`：`xcodebuild -workspace Together_ios.xcworkspace -scheme Together_ios -destination id=<UDID> -configuration Debug -derivedDataPath build build` + install + launch
- 产物路径约定：`build/Build/Products/Debug-iphonesimulator/Together_ios.app`；BundleId `ymjr.com.Together-ios`
- 模拟器定位：`xcrun simctl location <UDID> set <lat>,<lng>`（本次设 太原市小店区 37.7365,112.5626）
- **结论：以后 iOS 改动必须用上面命令实编译，以 BUILD SUCCEEDED 为闭环标准，不要再写"未编译验证"。**
- 本次实编译结果：BUILD SUCCEEDED（覆盖 9-20~9-21 全部 location 相关改动 + 位置崩溃修复 + 广场附近排序）

## 「附近地点」不显示 · 根因与兜底（9-21 续3）

- 入口链路：`BasePostCreateViewController.openLocationPicker()` → present `LocationSearchViewController`（pageSheet .medium/.large）；`LocationSearchViewController` 已有完整 UI（标题「地点」+ 关闭、搜索框、当前位置卡片带「当前位置」标签、附近地点 section、地图选点兜底）。
- 现象根因：附近列表数据源是 `MKLocalPointsOfInterestRequest(center:radius:)`。Apple 文档明确其"仅在区域内召回 POI"，中国区/模拟器无定位时召回稀疏或为空 → `nearby` 空 → 表头与行都不渲染（`viewForHeaderInSection` 有 `nearbyLoading || !nearby.isEmpty` 守卫），于是「附近地点」整块消失。
- 修复：
  1. `fetchNearby` 结果为空时，兜底 `fetchNearbyByKeyword`：按分类词 ["美食","咖啡","便利店","超市"] 并行 `MKLocalSearch.Request`（region 3km、resultTypes .pointOfInterest、filter .includingAll），`DispatchGroup` 合并 → `dedupeSort`（3km 内过滤 + 同名去重 + 距离升序 + 取前 20）。新增 `fallbackSearches` 强引用避免 search 被提前释放。
  2. `updateStatus` 增补空态提示：`currentPoint != nil && !nearbyLoading && nearby.isEmpty` → "附近暂无地点，可搜索或在地图上选取"。
- 未用 `regionPriority`（iOS 18+），保持 iOS 15 兼容。
