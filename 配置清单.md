# 艺启 · 配置清单（上线前必填）

> 所有"配置驱动"的逻辑已写好，**不配置不影响开发联调**，上线前按此清单补齐即可。
> 分三处：**后端 `.env`**（服务端能力）、**小程序 `src/config.ts`**（客户端指向）、**微信公众平台后台**（小程序资质与权限）。

---

## 一、后端配置（`TogetherCode/Together_api/.env`）

`.env.example` 为模板，复制为 `.env` 后填写。改完 `docker compose restart api` 生效。

### 1. 微信小程序（登录 + 支付 + 小程序码）
| 配置项 | 获取方式 | 不配置的影响 |
|---|---|---|
| `WX_APP_ID` | 微信公众平台 → 开发管理 → 开发设置 → AppID | 小程序一键登录不可用；**分享海报小程序码不可用**（返回"未配置"） |
| `WX_APP_SECRET` | 同上（AppSecret，需重置后复制） | 同上 |
| `WX_PAY_MCH_ID` | 微信支付商户平台 → 账户中心 → 商户信息 | 真实支付不可用（当前为模拟支付） |
| `WX_PAY_KEY` | 商户平台 → API 安全 → APIv3 密钥 | 支付回调验签跳过 |
| `WX_PAY_NOTIFY_URL` | 你的 HTTPS 域名 + `/v1/payments/wx-notify` | 支付回调无法到达 |

> 小程序码：配好 `WX_APP_ID/SECRET` 并重启后端后，`POST /v1/distribution/qrcode` 会调用微信 `wxa/getwxacodeunlimit` 生成真实带参码（scene=分享码，page 按帖子/课程自动选择），存 OSS/本地后海报页自动显示；未配置时海报页显示灰色占位框 + "配置微信后生成"。
> 注意：开发版小程序码 `env_version` 为 develop，需在微信开发者工具 / 体验版扫码才能打开；正式发布后为 release 自动生效。

### 2. 阿里云 OSS（图片/文件存储）
以下 **5 项配齐**后，`POST /v1/upload` 自动从本地存储切换为 OSS；留空则写入本地 `UPLOAD_DIR`。

| 配置项 | 获取方式 |
|---|---|
| `OSS_REGION` | 阿里云 OSS 控制台 → Bucket 概览 → 地域（如 `oss-cn-hangzhou`） |
| `OSS_ACCESS_KEY_ID` | 阿里云 RAM 访问控制 → AccessKey |
| `OSS_ACCESS_KEY_SECRET` | 同上 |
| `OSS_BUCKET` | OSS 控制台 → Bucket 名称 |
| `OSS_PUBLIC_URL` | 可选，绑定 CDN/自定义域名后的访问地址；留空用默认 OSS 域名 |

### 3. 极光推送（离线推送）
| 配置项 | 获取方式 | 不配置的影响 |
|---|---|---|
| `JPUSH_APP_KEY` | 极光控制台 → 应用 → 应用信息 | 离线推送不生效，消息只走 WebSocket/轮询 |
| `JPUSH_MASTER_SECRET` | 极光控制台 → 应用 → 密钥 | 同上 |

### 4. 基础生产项（上线前必改）
| 配置项 | 说明 |
|---|---|
| `NODE_ENV=production` | 生产模式 |
| `PORT` | 服务端口（默认 3000） |
| `FRONTEND_URL` | 管理端/小程序前端地址，用于 CORS 与跳转 |
| `JWT_SECRET` | **必须替换**为强随机串，防令牌伪造 |
| `DB_*` / `REDIS_URL` | 生产数据库/Redis 连接 |

---

## 二、小程序配置（`TogetherCode/Together-miniprogram/src/config.ts`）

所有项集中在 `APP_CONFIG` 对象，改完需重新构建（`npm run dev:weapp` 自动生效）。

| 配置项 | 填写内容 | 不配置的影响 |
|---|---|---|
| `BASE_URL` | 后端 API 完整地址，上线为 `https://你的域名/v1` | 开发默认 `http://127.0.0.1:3001/v1`；上线必须改 https |
| `ENV` | `dev` / `test` / `prod` | 仅影响日志/埋点行为 |
| `WX_APP_ID` | 与后端 `WX_APP_ID` 一致 | 小程序一键登录按钮不可用 |
| `OSS_DOMAIN` | OSS/CDN 图片域名（与后端 `OSS_PUBLIC_URL` 一致） | 图片按后端返回原值展示（本地可用，OSS 后建议配） |
| `SUBSCRIBE_TPL_IDS.orderPaid` | 订阅消息模板 ID（订单支付成功通知） | 支付成功后不弹订阅授权 |
| `SUBSCRIBE_TPL_IDS.lessonRemind` | 订阅消息模板 ID（课时消耗/上课提醒） | 课时提醒订阅不可用 |
| `SUBSCRIBE_TPL_IDS.refundResult` | 订阅消息模板 ID（退款结果通知） | 退款结果订阅不可用 |
| `WS_URL` | 站内信 WebSocket 地址（自建 WS 或极光 WS 网关） | 未配置自动退回 30s 轮询拉通知，功能可用但不实时 |

---

## 三、微信公众平台后台（小程序侧，需人工操作）

1. **注册小程序**：微信公众平台 → 立即注册 → 小程序（需邮箱 + 主体认证）。拿到 AppID。
2. **订阅消息**：小程序后台 → 功能 → 订阅消息 → 公共模板库，搜索并选用 3 个模板：
   - 订单支付成功通知（对应 `orderPaid`）
   - 上课/课时提醒（对应 `lessonRemind`）
   - 退款结果通知（对应 `refundResult`）
   把模板 ID 填到 `config.ts`。
3. **服务器域名白名单**：小程序后台 → 开发管理 → 开发设置 → 服务器域名（**只支持 HTTPS/WSS**）：
   - request 合法域名：后端 API 域名（如 `https://api.yourdomain.com`）
   - uploadFile 合法域名：同上（图片上传）
   - downloadFile 合法域名：OSS/CDN 图片域名
   - socket 合法域名：WS 域名（`wss://...`）
4. **微信支付**（可选，需商户号）：小程序后台开通微信支付 → 商户平台获取 `WX_PAY_MCH_ID` / `WX_PAY_KEY`。

---

## 四、资源文件（图片）

| 资源 | 位置 | 规范 |
|---|---|---|
| tabBar 图标 ×8 | `src/assets/tabbar/` | 81×81 PNG，透明底；未选中 `#9a938a`、选中 `#2f5d45`；已放占位图，直接替换同名文件即可（`home`/`courses`/`plaza`/`mine` + `-active`） |

---

## 五、配置顺序建议

1. **微信小程序注册 + AppID**（最优先，阻塞真机）
2. 填后端 `.env` 的 `WX_APP_ID/SECRET` + 小程序 `config.ts` 的 `BASE_URL/WX_APP_ID`
3. 公众号后台配服务器域名白名单
4. 配置 OSS 5 项 + `OSS_DOMAIN`
5. 订阅消息模板申请 + 填模板 ID
6. 极光推送（如需离线通知）
7. 上线前：`JWT_SECRET` 换强随机串、`NODE_ENV=production`、替换 tabBar 图标

---

## 六、验证方式（配置后如何确认生效）

| 配置项 | 验证动作 |
|---|---|
| 微信登录 | 小程序真机 → 点微信一键登录，能授权并进入 |
| OSS | `POST /v1/upload` 上传一张图，返回 `storage:"oss"` 且 URL 为 OSS 域名 |
| 订阅消息 | 下单支付成功后弹出订阅授权框 |
| WS | 控制台 Network 出现 WS 连接（`/messages/ws/token` 正常返回） |
| 分享返利 | 发帖人分享 → 他人带链接下单支付 → 分享人收益中心余额增加 |
